# TradingView Bridge Worker

Planned responsibilities:

- Launch browser automation with a user-owned TradingView session
- Load Pine source into TradingView
- Trigger an export adapter that emits series, signals, and trades as JSON
- Persist captured artifacts into `data/artifacts`
- Return structured status back to the API and WebSocket layer
